// The marker discovery reads, and the file that decides where the build
// writes. Left at the default (dist) so the adapter's candidate is exercised
// rather than an override.
import { defineConfig } from 'astro/config';

export default defineConfig({});
